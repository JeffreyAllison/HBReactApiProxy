/*import React from 'react';

export default function WeatherList({ weatherData }) {
  return (
    <div>
      {weatherData.map(({ temp, clouds, humidity }, i) => (
        <div className="weather-listings" key={temp + clouds + i}>
          <h3>Temperature in Kelvin {temp}</h3>
          <p>Cloud Cover Percentage: {clouds}</p>
          <p>Humidity Percentage: {humidity}</p>
        </div>
      ))}
    </div>
  );
}*/
